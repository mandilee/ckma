<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="company_title">
        <item name="android:textSize">@dimen/company_text_size</item>
    </style>

    <style name="detail_title">
        <item name="android:textSize">@dimen/title_text_size</item>
    </style>

    <style name="detail_text">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:paddingBottom">@dimen/text_size</item>
    </style>

    <style name="horizontalRule">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/horizontal_rule_height</item>
        <item name="android:background">@color/horizontal_rule_color</item>
        <item name="android:layout_margin">@dimen/horizontal_rule_margin</item>
    </style>

</resources>
